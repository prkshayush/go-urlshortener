package constant

const(
	UrlShortenerPath ="/short"
	// new route for short url
	RedirectUrlPath = "/url/:code"
)

const (
	Database = "urlshortener"
	UrlCollection = "url"
)
//base url
const (
	BaseUrl = "localhost:8080"
)

const (
	BindError = "Something went wrong"
)